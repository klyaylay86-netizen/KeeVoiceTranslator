# Kee Voice Translator

Android voice translator prototype for Myanmar, Thai, Chinese and English.

The APK is built automatically with GitHub Actions. Open the Actions tab and select **Build Android APK** to see build status and download the `KeeVoiceTranslator-APK` artifact when the run succeeds.
